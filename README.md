# gesture_controlled_painter
A real-time gesture-controlled virtual painter built using MediaPipe and OpenCV, allowing users to draw and erase on the screen through finger distance–based hand gestures captured from a webcam.
